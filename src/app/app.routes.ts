import { Routes } from '@angular/router';
import { LandingPageComponent } from './components/landing-page/landing-page.component';  // Página principal

export const routes: Routes = [
  { path: '', component: LandingPageComponent },

];
